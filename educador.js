function load(){const data=JSON.parse(localStorage.getItem('mundoSingularDemo')||'[]');document.getElementById('sessions').textContent=data.length;document.getElementById('independentTotal').textContent=data.reduce((s,x)=>s+(x.independent||0),0);document.getElementById('supportedTotal').textContent=data.reduce((s,x)=>s+(x.supported||0),0);const body=document.getElementById('history');if(!data.length){body.innerHTML='<tr><td colspan="4">Nenhuma atividade concluída ainda.</td></tr>';return}body.innerHTML=[...data].reverse().map(x=>`<tr><td>${x.activity}</td><td>${x.independent||0}</td><td>${x.supported||0}</td><td>${x.date||''}</td></tr>`).join('')}function clearDemo(){localStorage.removeItem('mundoSingularDemo');load()}load();