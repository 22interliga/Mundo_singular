function load(){const data=JSON.parse(localStorage.getItem('mundoSingularDemo')||'[]');const independent=data.reduce((s,x)=>s+(x.independent||0),0),supported=data.reduce((s,x)=>s+(x.supported||0),0);document.getElementById('sessions').textContent=data.length;document.getElementById('independentTotal').textContent=independent;document.getElementById('supportedTotal').textContent=supported;const body=document.getElementById('history');if(!data.length){body.innerHTML='<tr><td colspan="6">Nenhuma atividade concluída ainda.</td></tr>';document.getElementById('insight').textContent='Realize algumas atividades para gerar uma leitura demonstrativa.';return}body.innerHTML=[...data].reverse().map(x=>`<tr><td>${x.profile||'Criança A'}</td><td>${x.activity}</td><td>${x.supportLevel||'A'}</td><td>${x.independent||0}</td><td>${x.supported||0}</td><td>${x.date||''}</td></tr>`).join('');const total=independent+supported;let text='Há poucas sessões para observar um padrão. Continue a aplicação de forma consistente.';if(total>=3){const rate=independent/total;if(rate>=.75)text='Nas sessões registradas, predominam respostas independentes. Considere observar se esse padrão se mantém em diferentes atividades e contextos.';else if(rate>=.4)text='As sessões mostram combinação de respostas independentes e respostas com apoio. Observe em quais atividades as pistas são mais necessárias.';else text='Nas sessões registradas, o apoio foi utilizado com frequência. Mantenha instruções claras e observe quais pistas favorecem a participação.'}document.getElementById('insight').textContent=text}function clearDemo(){localStorage.removeItem('mundoSingularDemo');load()}load();